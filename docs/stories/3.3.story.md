# Story 3.3 — Navegação no Fullscreen: Touch, Swipe, Teclado

**Epic:** 3 — Player Fullscreen
**Status:** Draft
**Prioridade:** Must
**Estimativa:** 2h

---

## User Story

Como usuário,
Quero navegar entre vídeos no fullscreen com toque, swipe e teclado,
Para ter controle intuitivo como no Instagram Stories.

---

## Acceptance Criteria

- [ ] **AC-1:** Touch areas invisíveis: 28% esquerda (prev) e 28% direita (next) da largura do container
- [ ] **AC-2:** Swipe horizontal > 50px: esquerda → próximo, direita → anterior
- [ ] **AC-3:** Teclado ArrowLeft → vídeo anterior
- [ ] **AC-4:** Teclado ArrowRight → vídeo próximo
- [ ] **AC-5:** Teclado Escape → fecha fullscreen
- [ ] **AC-6:** Loop infinito: último vídeo → primeiro; primeiro → último
- [ ] **AC-7:** Ao terminar vídeo (`onEnded`) → avança automaticamente
- [ ] **AC-8:** `useSwipeGesture.ts` hook implementado

---

## Tasks

- [ ] Criar `src/hooks/useSwipeGesture.ts`
- [ ] Integrar touch areas no `FullscreenPlayer.tsx`
- [ ] Keyboard listener com `useEffect` + `cleanup`
- [ ] Handler `onEnded` no vídeo

---

## Notas Técnicas

```typescript
// useSwipeGesture.ts
export function useSwipeGesture(
  onSwipeLeft: () => void,
  onSwipeRight: () => void,
  threshold = 50
) {
  const touchStart = useRef<number>(0);

  const handlers = {
    onTouchStart: (e: React.TouchEvent) => {
      touchStart.current = e.touches[0].clientX;
    },
    onTouchEnd: (e: React.TouchEvent) => {
      const delta = touchStart.current - e.changedTouches[0].clientX;
      if (Math.abs(delta) > threshold) {
        delta > 0 ? onSwipeLeft() : onSwipeRight();
      }
    },
  };

  return handlers;
}
```

```tsx
// Touch areas no FullscreenPlayer:
// <div style={{ position: 'absolute', left: 0, top: 0, width: '28%', height: '100%', zIndex: 1 }}
//      onClick={goPrev} />
// <div style={{ position: 'absolute', right: 0, top: 0, width: '28%', height: '100%', zIndex: 1 }}
//      onClick={goNext} />
```

---

## File List

- [ ] `src/hooks/useSwipeGesture.ts`
- [ ] `src/components/FullscreenPlayer.tsx` (atualizado)
