# Story 1.2 — Tipos TypeScript e Estrutura de Dados

**Epic:** 1 — Fundação do Projeto
**Status:** Draft
**Prioridade:** Must
**Estimativa:** 30min

---

## User Story

Como desenvolvedor,
Quero os tipos TypeScript definidos e o JSON de configuração criado,
Para ter uma base tipada sólida para todos os componentes.

---

## Acceptance Criteria

- [ ] **AC-1:** `src/types/index.ts` exporta `VideoItem` e `WidgetConfig` conforme spec
- [ ] **AC-2:** `src/data/videos.json` criado com 7 vídeos de exemplo (dados mock)
- [ ] **AC-3:** Todos os campos obrigatórios presentes em cada item do JSON
- [ ] **AC-4:** `settings` completo com todos os toggles e configurações

---

## Tasks

- [ ] Criar `src/types/index.ts` com interfaces TypeScript
- [ ] Criar `src/data/videos.json` com dados mock dos 7 produtos

---

## Tipos Exatos

```typescript
// src/types/index.ts

export interface VideoItem {
  id: string;
  videoUrl: string;
  posterUrl: string;
  product: {
    name: string;
    price: string;
    image: string;
    url: string;
  };
  whatsapp?: string;
}

export interface WidgetConfig {
  videos: VideoItem[];
  settings: {
    whatsappDefault: string;
    accentColor: string;
    autoplay: boolean;
    autoplayDelay: number;
    showArrows: boolean;
    showDots: boolean;
    showWhatsapp: boolean;
    showShare: boolean;
    showLike: boolean;
    addToCartMode: 'redirect';
    storeUrl: string;
  };
}
```

---

## Dados Mock (videos.json)

Usar os 7 produtos da loja `julia-gonitjo-pijamas.bagypro.com`:
1. Camisa Transpasse Cintos — R$ 299,99
2. Camisa Ombro a Ombro Regata — R$ 289,99
3. Colete Cropped Alfaiataria — R$ 279,99
4. Calça Reta Botões — R$ 369,99
5. Shorts Botões Crepe — R$ 189,99
6. Macacão Longo Sobreposto — R$ 459,99
7. Colete Cinto — R$ 259,99

(URLs de vídeo com placeholder `SEU_CLOUD` até Cloudinary configurado)

---

## File List

- [ ] `src/types/index.ts`
- [ ] `src/data/videos.json`
