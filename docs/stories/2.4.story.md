# Story 2.4 — Autoplay, Lazy Load e IntersectionObserver

**Epic:** 2 — Carrossel de Vídeos
**Status:** Draft
**Prioridade:** Must
**Estimativa:** 2-3h

---

## User Story

Como usuário mobile,
Quero que o widget carregue rápido e economize bateria,
Para não ter a experiência lenta ou travar o scroll da loja.

---

## Acceptance Criteria

- [ ] **AC-1:** Vídeo ativo toca automaticamente (mutado)
- [ ] **AC-2:** Todos os outros vídeos ficam pausados
- [ ] **AC-3:** Ao terminar o vídeo ativo → `swiper.slideNext()`
- [ ] **AC-4:** Lazy load: `data-src` → `src` apenas para ativo + 2 vizinhos
- [ ] **AC-5:** `preload="metadata"` para vizinhos (preload parcial)
- [ ] **AC-6:** IntersectionObserver: pausa tudo quando seção sai da viewport
- [ ] **AC-7:** IntersectionObserver: retoma vídeo ativo quando seção volta
- [ ] **AC-8:** `useVideoControl` hook implementado e usado no carrossel

---

## Tasks

- [ ] Criar `src/hooks/useVideoControl.ts`
- [ ] Criar `src/hooks/useIntersection.ts`
- [ ] Integrar hooks no `VideoCarousel.tsx`
- [ ] Implementar lazy load: substituir `data-src` por `src` no slide change

---

## Notas Técnicas

```typescript
// useVideoControl.ts
export function useVideoControl(videos: VideoItem[]) {
  const videoRefs = useRef<(HTMLVideoElement | null)[]>([]);

  const playActive = (index: number) => {
    videoRefs.current.forEach((video, i) => {
      if (!video) return;
      if (i === index) {
        // lazy load: mover data-src para src
        if (video.dataset.src && !video.src) {
          video.src = video.dataset.src;
        }
        video.play().catch(() => {});
      } else {
        video.pause();
        video.currentTime = 0;
      }
    });
  };

  const pauseAll = () => {
    videoRefs.current.forEach(v => v?.pause());
  };

  return { videoRefs, playActive, pauseAll };
}
```

```typescript
// useIntersection.ts
export function useIntersection(
  ref: RefObject<Element>,
  onEnter: () => void,
  onLeave: () => void
) {
  useEffect(() => {
    const observer = new IntersectionObserver(
      ([entry]) => entry.isIntersecting ? onEnter() : onLeave(),
      { threshold: 0.1 }
    );
    if (ref.current) observer.observe(ref.current);
    return () => observer.disconnect();
  }, [ref, onEnter, onLeave]);
}
```

---

## File List

- [ ] `src/hooks/useVideoControl.ts`
- [ ] `src/hooks/useIntersection.ts`
- [ ] `src/components/VideoCarousel.tsx` (atualizado)
- [ ] `src/components/VideoSlide.tsx` (atualizado com videoRef)
