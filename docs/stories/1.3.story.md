# Story 1.3 — Utils: formatPrice e whatsapp

**Epic:** 1 — Fundação do Projeto
**Status:** Draft
**Prioridade:** Must
**Estimativa:** 20min

---

## User Story

Como desenvolvedor,
Quero utilitários de formatação de preço e geração de URL WhatsApp,
Para reutilizar em ProductCard, FullscreenProduct e FullscreenActions.

---

## Acceptance Criteria

- [ ] **AC-1:** `src/utils/formatPrice.ts` exporta função que formata número para "R$ X.XXX,XX"
- [ ] **AC-2:** `src/utils/whatsapp.ts` exporta função que gera URL `wa.me` com mensagem de produto
- [ ] **AC-3:** Ambas funções têm tipos TypeScript corretos

---

## Tasks

- [ ] Criar `src/utils/formatPrice.ts`
- [ ] Criar `src/utils/whatsapp.ts`

---

## Implementação Exata

```typescript
// src/utils/formatPrice.ts
export function formatPrice(price: string): string {
  return price; // já vem formatado do JSON: "R$ 299,99"
}
```

```typescript
// src/utils/whatsapp.ts
export function generateWhatsAppUrl(
  phone: string,
  productName: string,
  productPrice: string,
  productUrl: string
): string {
  const message = encodeURIComponent(
    `Olá! Tenho interesse no produto: ${productName} por ${productPrice}\n${productUrl}`
  );
  return `https://wa.me/${phone}?text=${message}`;
}
```

---

## File List

- [ ] `src/utils/formatPrice.ts`
- [ ] `src/utils/whatsapp.ts`
